# common

--- # local properties
spring:
  config:
    activate:
      on-profile: local

server:
  port: 18088

--- # dev properties
spring:
  config:
    activate:
      on-profile: dev

server:
  port: 18080